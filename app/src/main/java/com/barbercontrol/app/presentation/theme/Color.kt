package com.barbercontrol.app.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Paleta principal ──────────────────────────────────────────────────────────
// Identidade visual do Barber Control: tons escuros com dourado/âmbar como destaque

val PrimaryDark = Color(0xFF1A1A2E)       // Azul muito escuro (background principal)
val PrimaryVariant = Color(0xFF16213E)    // Variante do primário
val SecondaryDark = Color(0xFF0F3460)     // Azul profundo
val AccentAmber = Color(0xFFE94560)       // Vermelho/Rosa de destaque
val GoldAccent = Color(0xFFFFD700)        // Dourado para elementos premium

// ── Superfícies ───────────────────────────────────────────────────────────────
val SurfaceDark = Color(0xFF1E1E2E)
val SurfaceVariantDark = Color(0xFF2A2A3E)
val OnSurfaceDark = Color(0xFFE0E0E0)

// ── Estado ────────────────────────────────────────────────────────────────────
val ErrorRed = Color(0xFFCF6679)
val SuccessGreen = Color(0xFF4CAF50)
val WarningAmber = Color(0xFFFFC107)
